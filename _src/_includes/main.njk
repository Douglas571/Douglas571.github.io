<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Douglas jc - {{ title }}</title>

	<link rel="stylesheet" type="text/css" href="/css/styles.css">
</head>
<body>
    <div class="warning">
        <img width="32px" height="32px" src="{{ icons.warning }}">
        <h4>
            !Este sitio está en construcción!... pero igual puedes pasar y ver :D
        </h4>
    </div>
        
    <header id="header">
        <h1 class="logo">Douglas JC</h1>

        <nav id="nav-menu">
            <ul>
                {% for p in collections.forMainNav %}
                    <li>
                        {% if page.url !== p.url %}
                            <a href="{{ p.url }}">{{ p.data.title }}</a>
                        {% else %}
                            {{ p.data.title }}
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        </nav>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer id="footer">
        <p class="t-center">&copy; 2021</p>
    </footer>
</body>
</html>